<!-- File with the button redirect to PagSeguro  -->

<form action="{{ action }}" method="post">
    <div class="buttons">
    <div class="right">
      <input type="submit" value="{{ button_confirm }}" class="button" />
    </div>
  </div>
     <input type="hidden" id="url_ps" name="url_ps" value="{{ url_ps }}">
</form>